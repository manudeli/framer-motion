import { TargetAndTransition } from "framer-motion";

export const selectedListStyle: TargetAndTransition = {
  scale: 1.2,
  originX: 0,
  color: "#ffffff",
  textShadow: "0 0 8px #f8e112",
  fontWeight: "bold",
} as const;
